@{
    ViewData["Title"] = "Đã gửi Email";
    Layout = null; /* Ngắt layout chính */
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- COPY Y NGUYÊN CSS TỪ TRANG LOGIN/FORGOT PASSWORD --- */

        /* 1. Background */
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #a0c4ff;
            background-image: radial-gradient(at 0% 0%, hsla(253,16%,7%,0) 0, transparent 50%), radial-gradient(at 50% 0%, hsla(225,39%,30%,0) 0, transparent 50%), radial-gradient(at 100% 0%, hsla(339,49%,30%,0) 0, transparent 50%);
            background: linear-gradient(120deg, #89f7fe 0%, #66a6ff 100%);
            overflow-x: hidden;
        }

        /* 2. Card */
        .login-card {
            background: #ffffff;
            width: 90%;
            max-width: 500px;
            border-radius: 20px;
            padding: 2.5rem 3rem;
            box-shadow: 0 20px 60px rgba(0, 50, 100, 0.15);
            position: relative;
            z-index: 10;
            text-align: center; /* Căn giữa toàn bộ nội dung cho trang thông báo */
        }

        /* 3. Typography */
        h2 {
            font-size: 1.75rem;
            font-weight: 700;
            color: #1a1a1a;
            margin: 0 0 1rem 0;
        }

        .sub-text {
            color: #6c757d;
            font-size: 0.95rem;
            margin-top: 0.5rem;
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        /* 4. Button (Biến thẻ a thành nút) */
        .btn-primary-custom {
            display: inline-block; /* Để thẻ a hoạt động như nút */
            width: 100%;
            padding: 14px;
            background: linear-gradient(to right, #4facfe 0%, #00f2fe 100%);
            border: none;
            border-radius: 10px;
            color: white;
            font-size: 1.05rem;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none; /* Bỏ gạch chân */
            transition: transform 0.2s, box-shadow 0.2s;
            box-shadow: 0 10px 20px rgba(79, 172, 254, 0.3);
            box-sizing: border-box;
        }

            .btn-primary-custom:hover {
                transform: translateY(-2px);
                box-shadow: 0 15px 25px rgba(79, 172, 254, 0.4);
                background: linear-gradient(to right, #439cfb 0%, #00d2fe 100%);
                color: white;
            }

        /* 5. Icon Wrapper (Đổi màu nhẹ để báo hiệu Success) */
        .icon-wrapper {
            width: 80px; /* To hơn xíu cho trang confirm */
            height: 80px;
            background-color: #d1e7dd; /* Màu xanh lá nhạt */
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem auto;
            color: #198754; /* Màu xanh lá đậm */
            font-size: 2rem;
        }

        /* 6. Footer Link */
        .form-footer {
            margin-top: 1.5rem;
            font-size: 0.9rem;
            color: #666;
        }

            .form-footer a {
                color: #4facfe;
                font-weight: 600;
                text-decoration: none;
            }

                .form-footer a:hover {
                    text-decoration: underline;
                }

        /* Hiệu ứng nhỏ cho icon bay bay */
        @@keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-10px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        .icon-animation {
            animation: float 3s ease-in-out infinite;
        }

    </style>
</head>
<body>

    <div class="login-card">

        <div class="icon-wrapper icon-animation">
            <i class="fas fa-envelope-open-text"></i>
        </div>

        <h2>Kiểm tra Email</h2>

        <p class="sub-text">
            Chúng tôi đã gửi hướng dẫn đặt lại mật khẩu đến email của bạn.<br>
            Vui lòng kiểm tra hộp thư đến (và cả mục <b>thư rác</b> nếu không thấy).
        </p>

        <a asp-action="Login" asp-controller="Auth" class="btn-primary-custom">
            <i class="fas fa-sign-in-alt mr-2"></i> Quay lại Đăng nhập
        </a>

        <div class="form-footer">
            Chưa nhận được email?
            <a asp-action="ForgotPassword" asp-controller="Auth">Gửi lại</a>
        </div>

        <div style="margin-top: 10px; font-size: 0.8rem; color: #999;">
            Link sẽ hết hạn sau 15 phút.
        </div>

    </div>

</body>
</html>